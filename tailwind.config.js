/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{ts,tsx}"], // Scan TSX files in src
  theme: {
    extend: {}, // Add customizations here later if needed
  },
  plugins: [],
};
